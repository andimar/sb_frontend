<section id="sacramenti" class="sb-section sb-sacraments-section" aria-labelledby="sacraments-title">
    {% include '/markup/common/section-title.twig' with { section_title: 'SACRAMENTI', section_title_id: 'sacraments-title', section_title_class: 'sb-section-title--light' } %}
        
    <div id="sacramenti-list" class="sb-card-grid sb-section-content">
        {% for item in sacramenti %}
            <div>
                {% include '/markup/sacramenti/card.twig' %}
            </div>
        {% endfor %}
    </div>
</section>
