<div class="sacramenti-card">
    <h3 class="sacramenti-card-title">{{ item.title }}</h3>

    <div class="sacramenti-card-actions">
        {% for action in item.actions %}
            <a href="{{ action.link }}">{{ action.label }}</a>
        {% endfor %}
    </div>
</div>
