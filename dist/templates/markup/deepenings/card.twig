<div class="sacramenti-card grid">
    <h3 class="sacramenti-card-title col-12 col-middle">{{ item.title }}</h3>

    <div class="col-12 col-bottom">
        {% for action in item.actions %}
            <a href="{{ action.link }}">{{ action.label }}</a>
        {% endfor %}
    </div>
</div>
