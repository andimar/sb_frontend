<section id="christianlife" class="sb-section sb-christian-life-section" aria-labelledby="christian-life-title">

	{% include '/markup/common/section-title.twig' with { section_title: 'Percorsi di Vita Cristiana', section_title_id: 'christian-life-title', section_title_class: 'sb-section-title--red' } %}

	<div id="christianlife-list" class="sb-christian-life-grid sb-section-content">
        {% for item in christianlife %}
            <div>
                {% include '/markup/christianlife/card.twig' %}
            </div>
        {% endfor %}
    </div>	
</section>
